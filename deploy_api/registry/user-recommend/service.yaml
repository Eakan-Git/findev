apiVersion: v1
kind: Service
metadata:
  name: user-recommend-service
  namespace: development
spec:
  selector:
    app: user-recommend-deployment
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
