apiVersion: v1
kind: Service
metadata:
  name: user-recommend-canary-service
  namespace: development
spec:
  selector:
    app: user-recommend-canary-deployment
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
