apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: development
spec:
  selector:
    app: search-deployment
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
